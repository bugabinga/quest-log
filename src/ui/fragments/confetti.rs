use maud::{Markup, html};

/// Renders confetti celebration particles.
/// Currently a placeholder; actual confetti implementation can be added later.
#[must_use]
pub fn confetti() -> Markup {
    html! {
        // Confetti particles can be added here
        // For now, empty to avoid breaking compilation
    }
}
