#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
pre-push:
  jobs:
    - run: just fmt
    - run: just check
pre-commit:
  jobs:
    - run: just fmt
    - run: just lint
    - run: just test
